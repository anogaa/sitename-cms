'use strict';

/**
 * scorecard-subcategory router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::scorecard-subcategory.scorecard-subcategory');
