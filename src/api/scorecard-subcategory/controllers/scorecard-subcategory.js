'use strict';

/**
 * scorecard-subcategory controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::scorecard-subcategory.scorecard-subcategory');
