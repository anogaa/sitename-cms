'use strict';

/**
 * sitename-scorecard router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::sitename-scorecard.sitename-scorecard');
