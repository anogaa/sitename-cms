'use strict';

/**
 * sitename-scorecard service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::sitename-scorecard.sitename-scorecard');
